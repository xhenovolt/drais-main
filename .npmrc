# nodejs-mobile-react-native (devDep, Android runtime source) declares a
# react-native peer we never install for web builds. Vercel/CI run plain
# npm install, which hard-fails on that peer chain — resolve legacy-style.
legacy-peer-deps=true
